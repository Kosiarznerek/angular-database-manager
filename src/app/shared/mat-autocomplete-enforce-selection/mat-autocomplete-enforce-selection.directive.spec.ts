import {MatAutocompleteEnforceSelectionDirective} from './mat-autocomplete-enforce-selection.directive';

describe('MatAutocompleteEnforceSelectionDirective', () => {
  it('should create an instance', () => {
    const directive = new MatAutocompleteEnforceSelectionDirective();
    expect(directive).toBeTruthy();
  });
});
